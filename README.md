# Dockerized Python Pipeline

This project demonstrates a simple data processing pipeline using Python and Docker.

## How to run
docker build -t python-pipeline.

docker run python-pipeline

## Tech Stack
- Python
- Pandas
- Docker
